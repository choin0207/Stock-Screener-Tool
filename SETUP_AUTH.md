# 🔐 登入功能設定教學（Google Sheet 帳密後台）

設定完成後，開啟選股雷達網頁需輸入帳號密碼；可通行的帳密清單放在**您私人的
Google Sheet**（不會公開、不會存進 GitHub），之後直接在表格中增刪帳號即可。

> 💡 用手機操作時，遇到按鈕顯示不出來，請用瀏覽器選單的「切換為電腦版網站」。

## 步驟 1：建立帳號密碼表

1. 開啟 [sheets.new](https://sheets.new) 建立新的 Google 試算表，命名例如「選股雷達帳號」
2. 第 1 列輸入標題：A1 填 `帳號`、B1 填 `密碼`
3. 從第 2 列開始輸入允許登入的帳號密碼，例如：

   | 帳號 | 密碼 |
   |------|------|
   | user1 | mypassword123 |
   | family | abc12345 |

⚠️ 這份試算表**不要**開啟任何共用／發佈，維持私人即可。

## 步驟 2：加入驗證程式（Apps Script）

1. 在試算表上方選單點「**擴充功能 → Apps Script**」
2. 刪除編輯器內預設的程式碼，貼上本 repo
   [`google-apps-script/Code.gs`](google-apps-script/Code.gs) 的**全部內容**，按 💾 儲存

## 步驟 3：部署為網頁應用程式

1. 按右上角「**部署 → 新增部署作業**」
2. 點左邊齒輪 ⚙️ 選「**網頁應用程式**」
3. 設定：
   - 執行身分：**我**（您的帳號）
   - 具有存取權的使用者：**任何人**
4. 按「**部署**」→ 第一次會要求授權：選您的 Google 帳號 →
   出現「Google 尚未驗證這個應用程式」時點「**進階 → 前往（不安全）**」→ 允許
   （這是您自己寫的程式，安全無虞）
5. 複製產生的「**網頁應用程式網址**」（`https://script.google.com/macros/s/…/exec`）

## 步驟 4：把網址填入網頁設定

1. 開啟 GitHub 上的設定檔：
   `github.com/<您的帳號>/Stock-Screener-Tool/edit/main/docs/config.js`
2. 把複製的網址貼進 `AUTH_URL` 的引號內：

   ```js
   window.APP_CONFIG = {
     AUTH_URL: "https://script.google.com/macros/s/XXXXXXXX/exec"
   };
   ```

3. 按「**Commit changes**」存檔
4. 等 1–2 分鐘 GitHub Pages 重新部署，重新整理選股雷達網頁 → 出現登入畫面 🎉

## 日常管理

- **新增/移除使用者**：直接編輯 Google Sheet 的列即可，立即生效
- **登入有效期**：登入一次 7 天內免重登；按網頁右上「登出」可立即登出
- **修改驗證程式後**：需在 Apps Script 重新「部署 → 管理部署作業 → ✏️ → 新版本 → 部署」
- **關閉登入功能**：把 `config.js` 的 `AUTH_URL` 改回空字串 `""`

## 安全性說明（請務必了解）

- 帳密比對在 Google 端（Apps Script）執行，Sheet 保持私人，帳密不會出現在
  GitHub 或網頁原始碼中；連錯 5 次會鎖定 10 分鐘
- 但本專案的 repo 與資料檔（篩選結果 JSON）是公開的——登入機制擋的是
  「進入網頁介面」，直接存取資料檔網址的人仍能看到篩選結果。
  適合個人與親友使用；若需要真正的資料保密，需改用私有 repo + 付費方案
  或自架伺服器
- 請勿在此系統重複使用您其他重要服務（銀行、email）的密碼
